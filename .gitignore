# Dependencies
node_modules/
.pnpm-store/

# Builds & tool caches
dist/
build/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example

# Logs & OS
*.log
.DS_Store

# IDE
.idea/
.vscode/
*.local

# Terraform — state may contain secrets; providers are large and re-downloadable
.terraform/
*.tfstate
*.tfstate.*
# May carry environment-specific values; the .example is the committed template.
terraform.tfvars
.terraform.lock.hcl
crash.log

# Personal Claude Code permissions. settings.json is the shared, read-only set;
# anything that mutates infrastructure stays on the machine that granted it.
.claude/settings.local.json
